meta {
  name: Update-song
  type: http
  seq: 12
}

patch {
  url: {{base-url}}/song/365
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "lyrics": "HER"
  }
}

settings {
  encodeUrl: true
}
