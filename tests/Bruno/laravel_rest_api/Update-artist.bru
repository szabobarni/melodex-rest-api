meta {
  name: Update-artist
  type: http
  seq: 16
}

patch {
  url: {{base-url}}/artist/15
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "description": "Welcome to Mollywood"
  }
}

settings {
  encodeUrl: true
}
